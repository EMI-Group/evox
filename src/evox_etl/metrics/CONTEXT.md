# evox_etl/metrics — multi-objective quality indicators

## Intent
Pure `@etl.defn` functions: `igd(objs, pf, p)`, `igd_plus`, `gd`, `gd_plus`,
`hypervolume` (Monte-Carlo HV like torch evox). Mirror the math in
`../../evox/metrics/` (read-only sibling). See `../DESIGN.md`.
